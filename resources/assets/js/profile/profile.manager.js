var ProfileManager = (function ($) {
    var ProfileManager = function () {

        

        var init = function () {
            //todo:
            new ProfileExpenseWidget();
        };

        init();
        return {};
    };
    
    return ProfileManager;
})($);